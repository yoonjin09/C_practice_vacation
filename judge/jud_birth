#include <stdio.h>

int main(void) {
  int birth_year;
  int age;
  printf("Birth year? ");
  scanf("%d",&birth_year);
  age=2019-birth_year+1;
  if(age>19){
    printf("미성년자가 아닙니다.");
  }
  else printf("미성년자입니다.");
  return 0;
}
